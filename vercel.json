{
  "outputDirectory": ".",
  "installCommand": "echo 'Installing dependencies...' && npm install --production",
  "buildCommand": "echo 'No build step required for static site'",
  "rewrites": [
    { "source": "/admin", "destination": "/admin.html" }
  ]
}
